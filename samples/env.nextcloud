NEXTCLOUD_ADMIN_USER=you
NEXTCLOUD_ADMIN_PASSWORD=yourpassword
NEXTCLOUD_TRUSTED_DOMAINS=domainA domainB ...

POSTGRES_DB=postgres
POSTGRES_USER=nextcloud
POSTGRES_PASSWORD=(something like uuidgen, must match value in scripts/postgresql.sql)
POSTGRES_HOST=postgres

REDIS_HOST=redis

SMTP_HOST=(smtp server address)
SMTP_SECURE=ssl
SMTP_NAME=(username)
SMTP_PASSWORD=(password)
SMTP_PORT=465
MAIL_FROM_ADDRESS=noreply
MAIL_DOMAIN=(domain)
